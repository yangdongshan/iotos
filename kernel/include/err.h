#ifndef __ERRNO_H
#define __ERRNO_H

typedef enum {
    ERR_OK = 0,
    ERR_INV_ARG,
    ERR_NO_MEM,

    ERR_IN_INT,
    ERR_INT_NESTED,
    ERR_SCHED_LOCKED,

	ERR_MUTEX_MAGIC,
	ERR_MUTEX_TIMEOUT,
	ERR_MUTEX_BUSY,
	ERR_MUTEX_NESTED,
	ERR_MUTEX_OVERFLOW,
	ERR_MUTEX_HOLDER,
} errno_t;

#endif // __ERRNO_H

